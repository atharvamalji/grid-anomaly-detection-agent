version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/data:/app/data

  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"
  # Needs frontend/Dockerfile before this can be enabled.
