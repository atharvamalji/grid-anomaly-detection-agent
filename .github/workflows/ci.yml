name: CI

on:
  push:
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: echo "TODO: pip install -e .[dev] once pyproject.toml exists"
      - name: Lint
        run: echo "TODO: ruff check ."
      - name: Test
        run: echo "TODO: pytest"

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        run: echo "TODO: npm ci once package.json exists"
      - name: Lint
        run: echo "TODO: npm run lint"
      - name: Test
        run: echo "TODO: npm test"

  docker-build:
    runs-on: ubuntu-latest
    needs: [backend]
    steps:
      - uses: actions/checkout@v4
      - name: Build backend image
        run: echo "TODO: docker build -t grid-agent-backend ./backend once Dockerfile exists"
